\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{aussehen/hbrs-inf}[2022/01/27 hbrs-inf theming Package]

% Enable the arial font
\RequirePackage{fontspec}
\setmainfont{Arial} % If an error tells you, that this font can't be found, please install it on your computer.

\RequirePackage{graphicx}
\RequirePackage{svg}
\RequirePackage{datetime}

% Einstellungen für die Präsentation
\usetheme{Ilmenau}
\title{\ThesisTitle}
\subtitle{\ThesisSubject}
\author{\ThesisAuthor}
\institute{Fachbereich Informatik - Hochschule Bonn-Rhein-Sieg}
\date{\ThesisPubDate}
\logo{\includegraphics[height=0.5cm]{bilder/fhlogo}}

% TODO Workaround if an texlive version older than 2021 is used, like in github actions. The file gets added by github-actions.
% Präsentationsnotizen auf 2. Bildschirm
% Programm pdfpc wird benötigt https://pdfpc.github.io/ https://github.com/pdfpc/pdfpc
\IfFileExists{aussehen/pdfpc.sty}{\RequirePackage{aussehen/pdfpc}}{\RequirePackage{pdfpc}}

\RequirePackage{hyperref}

% Metadaten für die PDF-Datei
\hypersetup{
    pdftitle={\ThesisTitle},
    pdfsubject={\ThesisSubject},
    pdfauthor={\ThesisAuthor},
    pdfcreator={XeLaTeX},
    pdfkeywords={\ThesisKeywords}
}

% Deutsch
\usepackage[ngerman]{babel}
\usepackage[ngerman]{isodate}

% Quellcode einfügen und highlighten
\RequirePackage[outputdir=out]{minted} % Needs python installed and to be loaded before csquotes

%% Literatur
% Zitate
\RequirePackage{csquotes}

% Literaturverzeichnis nach ISO 690 (Nachfolger von DIN 1505)
% Further help at https://github.com/michal-h21/biblatex-iso690
\usepackage[style=iso-authortitle]{biblatex}
\addbibresource{datenbanken/Literaturverzeichnis.bib}
\setbeamertemplate{bibliography item}{}

% Gantt-Diagramm unterstützung
\RequirePackage{pgfgantt}

% To-dos
% Fixes a warning if todonotes is used in a beamer documentclass
\setlength {\marginparwidth }{2cm}
\RequirePackage{todonotes}
% Beamer needs inline to-dos
\presetkeys{todonotes}{inline}{}
